package com.tests.chess.engine.board;

import com.chess.engine.board.Board;
import com.chess.engine.board.BoardUtils;
import com.chess.engine.board.Move;
import com.chess.engine.player.MoveTransition;

import static org.junit.Assert.*;

public class BoardTest {
/*
    public void TestFoolsMAte(){
        final Board board = Board.createStandardBoard();
        final MoveTransition t1 = board.currentPlayer()
                .makeMove(Move.MoveFactory.CreateMove(board, BoardUtils.;
                        BoardUtils.getPositionAtCoordinate("f3")));
        assertTrue(t1.getMoveStatus().isDone());

        final MoveTransition t2 = t1.getTransitionBoard()
                .currentPlayer()
                .makeMove(Move.MoveFactory.CreateMove(t1.getTransitionBoard(),BoardUtils.getPositionAtCoordinate("e7"),
                        BoardUtils.getPositionAtCoordinate("e5")));
    }
*/
}